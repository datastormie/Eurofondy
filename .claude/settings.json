{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "agent",
            "prompt": "This turn just finished. First, cheaply determine whether any file matching docs/**/*.html, docs/**/*.css, or docs/**/*.js has uncommitted changes (e.g. run `git status --porcelain -- docs` if you have shell access, or Glob those patterns and check whether the most recently modified file's timestamp is recent). If none do, respond with exactly \"Not applicable.\" and do nothing else - do not read or grep any files in that case. Otherwise, verify Montserrat remains the single typeface used consistently across ALL such files under docs/ (not only the ones that just changed), per this project's brand system: (1) every HTML page under docs/ that renders visible text loads Montserrat via the same Google Fonts <link> pattern used in docs/index.html; (2) docs/styles.css defines --font-display and --font-body as 'Montserrat', Arial, Helvetica, sans-serif; (3) every font-family declaration in styles.css or inline <style> blocks uses var(--font-display)/var(--font-body) or that exact stack (font-family: inherit is fine); (4) any Chart.js page sets Chart.defaults.font.family to that same stack; (5) only font weights 400/500/600/700 are used. Report violations found (file, line, the offending declaration, why it breaks consistency), or state explicitly that everything is consistent.",
            "timeout": 90,
            "statusMessage": "Checking Montserrat font consistency (docs/ changed this turn)..."
          },
          {
            "type": "agent",
            "prompt": "This turn just finished. First, cheaply determine whether any file matching scripts/fetch_*.py or .github/workflows/monthly.yml has uncommitted changes (e.g. run `git status --porcelain -- scripts .github/workflows/monthly.yml` if you have shell access). If none do, respond with exactly \"Not applicable.\" and do nothing else - do not read or grep any files in that case. Otherwise, review the changed fetch script(s) against this project's ITMS21 fetch script conventions (see .claude/skills/eurofondy-itms21-fetch/SKILL.md for full detail): (1) every requests.get call wrapped in a retry loop with RETRY_ATTEMPTS=3, RETRY_BACKOFF_SECONDS=2 backoff; (2) ThreadPoolExecutor(max_workers=8) for detail fetches; (3) the shared dotted-path _get() helper; (4) itms21_-prefixed, lowercase table/column names, with schema set via CREATE SCHEMA IF NOT EXISTS + SET schema='slovakia' (or 'website' for the two current-state tables) right after connecting; (5) get_known_ids()-based gating so already-stored ids are never re-fetched - archetype #2/#3 tables use INSERT ... ON CONFLICT (id) DO NOTHING with no UPDATE path, only archetype #1 full-overwrite tables use ON CONFLICT DO UPDATE; (6) a periodic commit every 50 records (20 for per-category loops) plus a final commit; (7) TABLE_COMMENTS/COLUMN_COMMENTS dicts covering every table/column, applied via the shared apply_comments()/_esc() helpers; (8) if the script has no list endpoint and instead sources ids from another already-fetched table, a runtime guard that raises clearly if that source table doesn't exist yet; (9) the script is present in the 'Run fetch scripts' step of .github/workflows/monthly.yml, positioned after anything it depends on. Report violations found (file, line, the issue, why it breaks convention), or state explicitly that the script is consistent with the other fetch scripts.",
            "timeout": 90,
            "statusMessage": "Checking new/changed fetch script against ITMS21 conventions..."
          }
        ]
      }
    ]
  }
}
