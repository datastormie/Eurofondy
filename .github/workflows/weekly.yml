name: Weekly fetch of EU program data

on:
  schedule:
    - cron: "0 5 * * 1"
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  fetch-and-publish:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run fetch scripts
        run: |
          python scripts/fetch_programs.py
          python scripts/fetch_projects.py

      - name: Commit and push updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/program_data.json docs/project_data.json data/eurofondy.duckdb
          git diff --staged --quiet || git commit -m "Update program/project data $(date -u +'%Y-%m-%d %H:%M UTC')"
          git push